[package]
name = "daisy-bootloader"
version = "2.0.0"
edition = "2021"
description = "UEFI bootloader for the Daisy OS Rust microkernel"

[dependencies]
uefi = "0.33"
uefi-services = "0.27"
log = "0.4"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = "s"
lto = true
