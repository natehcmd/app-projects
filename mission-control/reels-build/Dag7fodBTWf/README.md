# Dag7fodBTWf — Nothing buildable/installable (re-reviewed via video+audio, 2026-07-20)

## Source content

> @nocodealex: "CREW" — set up Fable as 6 different AI employee personas
> (CMO, VP Sales, CTO, COO, Chief of Staff, CSO) with specific prompts, so
> they keep running your business autonomously. Comment "CREW" for the exact
> prompts + setup.

## Video review

Watched the clip — stylized "unlock the vault" game-graphic B-roll (a mobile
game clip used as a metaphor), talking-head narration. No actual prompts,
config files, repo, or product shown on screen — the entire technique (the
specific 6 prompts and "setup so they keep working") is exactly what's gated
behind the comment funnel.

## Why nothing was built

The underlying idea — multiple named AI personas with distinct roles,
consulted for a task — isn't secret or novel; it's a standard multi-persona
prompting pattern. We already have a working implementation of that same
concept, built from a different reel: `dm_32917943269963244995258185722888192`
("Idea Council" — runs multiple perspectives/personas over an idea). Building
a second, near-identical tool under a different name would be redundant
rather than a real reconstruction of anything this reel actually disclosed
(the specific 6 prompts were never shown).

## Status

`built = false`, `installed = false` — equivalent concept already covered by
Idea Council (`dm_32917943269963244995258185722888192`).

## Web research pass (2026-07-21)

Product named: 'Fable' (multi-agent business-employee platform, 6 named personas). No specific installable product/repo found via web search beyond generic 'Fable AI' branding ambiguity (multiple unrelated products use this name). The reel's own pitch is comment-gated with no real setup shown. Confirmed nothing concretely findable/installable — the described 6-persona workflow is a prompting *pattern*, not a specific product, and is already achievable via Claude's own subagent capability without needing a third-party product.
