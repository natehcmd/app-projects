# DYKSh1iv8nP — UNSAFE, do not install (Vayne AI)

## Source

> @nick_saraev: "Vayne AI" — scrapes LinkedIn Sales Navigator from the cloud.
> "No Chrome extension needed. It has the lowest fingerprint on LinkedIn...
> your account risks nothing." Pulls up to 15,000 profiles/day, splitting
> queries to bypass Sales Navigator's stated 2,500-profile search limit.

## Why this is declined regardless of legitimacy

This isn't a case of checking whether the product is "real" — the reel's
own description is the problem. Its core selling points are explicitly:
1. **Detection evasion** — "lowest fingerprint," "your account risks
   nothing" — built specifically to avoid LinkedIn's bot/scraping detection.
2. **Rate-limit/ToS circumvention** — deliberately splitting queries to
   exceed Sales Navigator's documented per-search limits.

Both are things I don't build, install, or facilitate regardless of whether
the underlying product is a real, functioning SaaS tool (it may well be) —
this is bot-detection evasion and ToS-violating scraping by design, which is
the same category already excluded from the original 33 "unsafe" reels this
session. Not investigating further, not installing, not building a "safer"
local equivalent (a detection-evasion scraper doesn't have a safe version —
the evasion IS the product).

## Status

**UNSAFE — do not install.** No code written, no research beyond confirming
the product's own stated purpose from its own marketing.
