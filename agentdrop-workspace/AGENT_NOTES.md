# Agent shared notes

- 2026-07-09: Workspace was deleted by a runaway agent-fleet session and
  rebuilt from a restore. Prior notes are lost. Media in reels/ re-syncs
  automatically via ig-curate.py (launchd, every 10 min); the whisper model
  needs re-downloading to .models/ggml-base.en.bin before "listen" works.
- 2026-07-09: New rule — any reel/short link a job touches gets added to the
  Library first via ./add-to-library.sh (see CLAUDE.md).
